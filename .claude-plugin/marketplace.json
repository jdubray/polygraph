{
  "name": "polygraph",
  "owner": {
    "name": "jdubray",
    "url": "https://github.com/jdubray"
  },
  "plugins": [
    {
      "name": "polygraph",
      "source": "./",
      "description": "A polygraph for your state machine. The agent does the hard part for you: it instruments your code, builds any test doubles needed to run it, and captures real execution traces — then has an LLM derive a transition-function spec from the source (default: a SAM v2 strict-profile module with named intents/schemas/domains, keyed acceptors, observable reject(reason), sealed model, next-state (prime) acceptors; --legacy-bare-next keeps the bare next(state, action, data) artifact), replays the traces against it (conformance), model-checks it against invariants (finds bugs by exhaustive iteration), and can escalate the winning spec to TLC via mechanical TLA+ transpilation (--tla), surfacing every disagreement as a spec-error, code-finding, or contract-error. Also includes polygen (author NEW verifiable code from a feature description, self-repaired against reachable invariant violations before it ships) and polyvers (version a state machine with mechanical compatibility gates: classify the change into lanes, replay old-version stimuli and validate migrations against fleet snapshots, and model-check whether any live state can be driven to an invariant violation under the new rules — no API key), and polynv (elicit the invariants themselves: harvest candidates from the contract vocabulary, traces, and fleet snapshots, pre-check each against the machine, drive a plugin-led interview into an attributed intent ledger, and grade invariant-set strength by mutation — no API key except the optional --llm source).",
      "version": "6.2.1",
      "author": {
        "name": "jdubray",
        "url": "https://github.com/jdubray"
      }
    },
    {
      "name": "polyviz",
      "source": "./polyviz",
      "description": "OPTIONAL companion to polygraph — the visualization side of the method. A deterministic, artifact-derived diagram renderer: turns the artifacts the suite already produces (the state machine, the invariants, the counterexample, the polyvers compat verdict) into a fixed catalog of clean, brand-consistent SVGs (optional PNG). Same inputs → byte-identical output; no model call at render time. Renders from a viz-model JSON (executes no user code, no native deps) or straight from a Polygraph/polyvers artifacts directory (adapters derive the machine graph by bounded BFS over the module, read the invariants, resolve a failing findings.json to its counterexample trace, and map compat-report.json). Install separately from polygraph: the state-machine graph needs the optional elkjs and PNG needs the optional @resvg/resvg-js (both lazy); the pure viz-model → SVG path needs neither and runs no user code.",
      "version": "0.1.0",
      "author": {
        "name": "jdubray",
        "url": "https://github.com/jdubray"
      }
    }
  ]
}
