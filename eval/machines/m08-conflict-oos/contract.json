{
  "lang": "javascript",
  "stateKeys": [
    { "name": "status", "type": "'pending' | 'active'" }
  ],
  "initState": { "status": "pending" },
  "actions": {
    "CHARGE": { "dataFields": { "result": "'ok' | 'declined' | 'conflict'" } }
  },
  "terminalStates": [],
  "specialRules": [
    { "name": "conflict-handling", "note": "How an idempotency conflict is handled — but whether it is CORRECT depends on the external processor's replay semantics, which the code cannot settle about itself.", "whenAction": "CHARGE" }
  ],
  "noOpRule": "CHARGE on an already-active row yields post == pre."
}
