{
  "lang": "javascript",
  "stateKeys": [
    { "name": "status", "type": "enum: 'authorized' | 'captured' | 'pending' | 'requires_more' | 'error' | 'canceled'" },
    { "name": "authorizedAt", "type": "boolean — ABSTRACTION of authorized_at; the code tests presence only" },
    { "name": "hasPayment", "type": "boolean — ABSTRACTION of a linked Payment row; the code tests presence only" }
  ],
  "initState": { "status": "pending", "authorizedAt": false, "hasPayment": false },
  "actions": {
    "PROVIDER_INITIATE": { "dataFields": { "providerStatus": "string" } },
    "PROVIDER_UPDATE": { "dataFields": { "providerStatus": "string" } },
    "PROVIDER_AUTHORIZE": { "dataFields": { "providerStatus": "string" } }
  },
  "dataDomain": {
    "PROVIDER_INITIATE": { "providerStatus": ["authorized", "captured", "pending", "requires_more", "error", "canceled"] },
    "PROVIDER_UPDATE": { "providerStatus": ["authorized", "captured", "pending", "requires_more", "error", "canceled"] },
    "PROVIDER_AUTHORIZE": { "providerStatus": ["authorized", "captured", "pending", "requires_more", "error", "canceled"] }
  },
  "terminalKey": "status",
  "terminalStates": [],
  "specialRules": [
    { "name": "already-authorized", "note": "authorizePaymentSession short-circuits when a linked Payment exists AND authorized_at is set. Note it keys on those two fields, NOT on the status value — the only guard in this machine, and it does not guard on status.", "whenState": "status = 'authorized'", "whenAction": "PROVIDER_AUTHORIZE" }
  ],
  "noOpRule": "An action that does not apply is reject(reason) — an observable no-op. IMPORTANT: the non-authorized branch of authorizePaymentSession is NOT a no-op. It WRITES the provider's status and then raises a MedusaError. The row is persisted; the throw is control flow for the caller. Modelling it as a rejection would falsely imply the state did not change.",
  "trustBound": "The provider's reported status is written VERBATIM. packages/modules/payment/src/services/payment-module.ts performs no enum-membership check, no validation, and no mapping — the sole transformation anywhere is authorizePaymentSession_ collapsing CAPTURED into AUTHORIZED. Domain membership is enforced by the DATABASE (the model.enum column), not by the application, so a provider reporting an unknown status fails at write time as a constraint violation rather than being rejected in code.",
  "projectionBound": "These three keys are the ENTIRE observable state. Amounts, currency_code, the provider `data` blob, `context`/idempotency keys, the provider id, and the linked PaymentCollection and Payment entities are OUTSIDE the projection. They are passed to providers but never read to decide a session status. Every guarantee made here is relative to this list.",
  "upstream": {
    "repo": "medusajs/medusa",
    "tag": "v2.17.1",
    "commit": "9aadd07b63d005d57b912b202144ef3f96bf2c90",
    "sources": [
      "packages/core/utils/src/payment/payment-session.ts",
      "packages/core/types/src/payment/common.ts",
      "packages/core/types/src/payment/provider.ts",
      "packages/modules/payment/src/models/payment-session.ts",
      "packages/modules/payment/src/services/payment-module.ts",
      "packages/core/core-flows/src/payment/steps/authorize-payment-session.ts"
    ]
  }
}
