# Fleet study · Tier 2 — adjudication worksheet

Corpus: `C:\Users\jjdub\code\polygraph\examples\fleet-study-stripe\out` · provenance: simulated (offline: the machine generated this corpus — NOT admissible as Tier 2 evidence)
**Admissible as Tier 2 evidence: NO**

> This corpus was generated by the v1 machine itself, so it is circular
> as evidence about that machine. The worksheet below exercises the
> adjudication pipeline; its buckets must NOT be reported as Tier 2
> results. Re-run against a `--live` capture for admissible numbers.

## How to fill this in

Findings are listed in **shuffled order with no predicted verdict shown**, so a
judgment is made on the finding itself rather than on what the tool expected.
For each row write one bucket in the `bucket` column of `results.json`
(or here, then re-run with `--adjudicated`):

| bucket | meaning |
|---|---|
| **TP** | a true incompatibility: the new version genuinely mishandles a state the fleet holds |
| **TI** | true but intended: the finding is correct and the behaviour is deliberate (e.g. a strengthened invariant naming live violators). The human decides what those instances mean |
| **FP** | spurious: tool error — bad domain inference, an over-conservative superset, a BOUNDED run misread, a gate misfiring on a cosmetic change |

Record the judge, and resolve disagreements with a third reader; the count of
disagreements is part of the result.

**One defect can appear as several rows.** The gates are deliberately
overlapping views — a violating state is seen by `migrate` as an output it
produced, by `invariants-pointwise` as a state the fleet holds, and by
`semantic-model-check` as a root or a reachable state. Judge each row on its
own, then note which rows you consider the same underlying defect; the study
reports both counts, because per-gate volume and distinct-defect count answer
different questions (operator noise vs. detection power).

## Findings

| # | finding | witness | bucket (fill in) | note |
|---|---|---|---|---|
| 1 | the shape changed but the new version has no migrate.cjs — a failing round-trip in this lane needs a verified migration; start with `polyvers migrate scaffold` | — |  |  |
| 2 | the shape changed but the new version has no migrate.cjs — a failing round-trip in this lane needs a verified migration; start with `polyvers migrate scaffold` | — |  |  |
| 3 | migrated state violates 'exhausted-dunning-is-unpaid' | fleet.json#10 |  |  |
| 4 | 'dunning-within-budget' [state] — reachable state violates the rule; shortest counterexample from snapshot fleet.json#10: PAYMENT_FAILED({}) | fleet.json#10 |  |  |
| 5 | violates 'exhausted-dunning-is-unpaid' — this state exists (or is reachable); decide what it means BEFORE the deploy | fleet.json#10 |  |  |
| 6 | 'exhausted-dunning-is-unpaid' [state] — violated in a seeded initial state; shortest counterexample from snapshot fleet.json#10: (violated at the root state) | fleet.json#10 |  |  |

_The mapping from row number back to gate and change lives in `results.json`,
so it can be consulted after judging rather than before._
